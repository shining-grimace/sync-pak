import { ComboBox as StandardComboBox } from "std-widgets.slint";
import { Dimensions } from "../dimensions.slint";

export component ComboBox inherits StandardComboBox {
    height: Dimensions.button-height;
    vertical-stretch: 0;
}

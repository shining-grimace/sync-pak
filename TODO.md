
- There should probably be text input limits on things like Connection Name
- Outlining drop-down elements with the primary colour would look nice
- The desktop app seems a little sluggish; test this in release mode, else check if something can be done on the 0.5s or so lag between clicking a navigation button and the new layout appearing (is it async work to be done in the background while a loading indicator is shown in the UI?)
- Set up release config for Android
- Target latest Android
- Bump Rust and Android dependencies where possible
- Create branding logo
- Create privacy policy
- Review UI theming
